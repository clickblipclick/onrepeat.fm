/** Inline prose links: already accent-colored, so the hover cue is an underline (a
 *  color-shift would be a no-op). Neutral/muted interactive text keeps `hover:text-accent`. */
export const linkInline = 'text-accent hover:underline'
